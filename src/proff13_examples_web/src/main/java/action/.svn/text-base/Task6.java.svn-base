package action;

import java.util.ArrayList;

/*
extends HttpServlet
public method doGet
---------------------
пользователь вводит послндовательность чисел,
1)вывести на экран в обратном порятке
2)Увеличить все елементы на 10 и вывести
3)Заменить все четные на 0
4)Удалить все простые числа и вывести
5)Вывести на экран 2 половину последовательности
6)Вычеслить сумму всех елементов и среднее арифметическое

*/
public class Task6 {
  public ArrayList<Integer> sumAver(ArrayList<Integer> array){
      int sum = 0;
      for (int i = 0; i < array.size() ; i++) {
          sum+=array.get(i);

      }
      int average = 0;
      average = sum/array.size();
      ArrayList<Integer> result = new ArrayList<>();
      result.add(sum);
      result.add(average);
      return result;
  }
}
