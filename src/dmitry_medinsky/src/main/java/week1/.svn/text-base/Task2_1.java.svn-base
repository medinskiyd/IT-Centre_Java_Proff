package week1;

import java.io.BufferedInputStream;
import java.io.FileReader;
import java.io.IOException;
import java.util.ArrayList;


/**
 * Дан список целых чисел (0 - 20), размер списка пользователь вводит с клавиатуры.
 * Scanner не использовать.
 * - метод1 (все четные увеличить на 1);
 * - метод2 (все нечетные удалить);
 * - метод3 (отсортировать список по возрастанию и вывести на экран);
 *
 * для сортировки использовать Collections.sort();
 * Использовать методы: iterator(); add();
 */
public class Task2_1 {
    public static void main(String[] args) {

        int size = 0;

        ArrayList<Integer> arr = new ArrayList<Integer>(size);
        for (int i = 0; i < arr.size(); i++) {
            arr.add((int)Math.random()*20);
        }
        System.out.println(arr.toString());

//        System.out.println("Введите размер списка");
//        try (BufferedInputStream reader = new BufferedInputStream(System.in)) {
//
//                while(reader.read() != -1) {
//                System.out.println(reader.read());
//                System.out.println("Hello");
//                size += reader.read();
//            }
//            reader.close();
//        } catch (Exception e) {
//            e.printStackTrace();
//        }

    }
}
