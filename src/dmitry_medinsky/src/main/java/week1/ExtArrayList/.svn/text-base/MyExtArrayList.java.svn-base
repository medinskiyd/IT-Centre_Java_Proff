package week1.ExtArrayList;

import org.omg.CosNaming._BindingIteratorImplBase;

import java.util.Iterator;

/**
 * Created by Дмитрий on 18.01.14.
 *
 * Сделать параметризированную релизацию списка
 * Реализовать в списке интерфейсы Iterable и Iterator, не забывайте про ConcurentModificacionException
 */
public class MyExtArrayList //implements Iterable<T>, Iterator<E> {
{
    private String E = null;
    private int index = 0;
    private boolean decision = false;
    private int size = 0;
    private boolean decision1 = false;

    // Конструктор.
    public MyExtArrayList() {

    }

    // Добавление нового элемента в конец списка.
    public void add(String E) {
    }
    // Добавление нового элемента в определенное место в списке.
    public void add(int index, String E) {
    }
    // Удаление определнного элемента в списке.
    public void remove(int index) {

    }
    // Изменение определнного элемента в списке.
    public void set(int index, String E) {
    }
    // Узнать номер элемента в списке.
    public int indexOf(String E) {
        return index;
    }
    // Выявляет есть ли такой элемент в списке.
    public boolean contains(String E) {
        return decision;
    }
    // Размер списка.
    public int size() {
        return size;
    }
    // Удаляет все элементы из списка.
    public void clear() {
    }
    // Cравнивает два списка.
    boolean equals(MyExtArrayList list) {
        return decision1;
    }
    //

}
